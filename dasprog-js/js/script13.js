/* 
* Refactoring
    ? Readability
    ? DRY (Don't repeat Yourself)
    ? Testability
    ? Performence
    ? Maintainability */

function jumlahvolumeDuakubus(a, b){
    return a * a * a + b * b * b;
}

console.log(jumlahvolumeDuakubus(8, 3));
